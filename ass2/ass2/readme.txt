how to start client:

we start client in console, which we can input command in console, also it can be started by command line.



how ro start server:

it is very similar with starting client in terms of our apporach and a standard way, but there are
two more parameters than skeleton edition, which is -t indicating the type of server, and -n, the name of the server, specifically, "1" in type means master server, "2" is backup server for master server and "3" is client-oriented server.


Also, message category and form are also different from that in assignment 1 which can be checked in MessageType.java.